<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_undo_route_edit"
        android:icon="@drawable/ic_undo"
        android:title="@string/undo"
        app:contentDescription="@string/undo"
        app:tooltipText="@string/undo"
        app:showAsAction="always" />

    <item
        android:id="@+id/action_save_route"
        android:icon="@drawable/ic_save"
        android:title="@string/save_route"
        android:visible="false"
        app:contentDescription="@string/save_route"
        app:tooltipText="@string/save_route"
        app:showAsAction="always" />
</menu>
